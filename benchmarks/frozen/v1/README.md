# Frozen Benchmark v1

- このディレクトリのfixtureは quality gateで使用する凍結ベンチマーク。
- 更新時は `benchmarks/registry/manifest.v1.json` の version と artifactHash を更新し、reviewer承認を必須とする。
- contaminationRisk が `high` のfixtureは hard gate判定に使わない。
